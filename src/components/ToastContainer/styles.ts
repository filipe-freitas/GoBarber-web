import styled from 'styled-components';

export const Container = styled.div`
  position: absolute;
  /** Posiciona o container de Toasts */
  right: 0;
  top: 0;
  /** Não entendi muito bem a necessidade */
  padding: 30px;
  overflow: hidden;
`;
